[
  #foreach( $phone in $patient.getPhoneNumbers()  )
    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $phone) )
      {"regexp": "(?i)$approximateMatch", "placeHolder": "FFFFF"},
    #end
  #end
]
