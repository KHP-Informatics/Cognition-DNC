[

#foreach( $forename in $patient.getForeNamesInDescendingLengthOrder()  )
      #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $forename) )
        {"regexp": "(?i)$approximateMatch", "placeHolder": "XXXXX"},
      #end
  #end

  #foreach( $surname in $patient.getLastNamesInDescendingLengthOrder()  )
    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $surname) )
    {"regexp": "(?i)$approximateMatch", "placeHolder": "XXXXX"},
    #end
  #end

  #foreach( $forename in $patient.getCarerForeNames()  )
    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $forename) )
    {"regexp": "(?i)$approximateMatch", "placeHolder": "YYYYY"},
    #end
  #end

  #foreach( $surname in $patient.getCarerLastNames()  )
    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $surname) )
    {"regexp": "(?i)$approximateMatch", "placeHolder": "YYYYY"},
    #end
  #end

]
