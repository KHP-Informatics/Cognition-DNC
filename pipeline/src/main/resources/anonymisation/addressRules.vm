[
  #foreach( $address in $patient.getAddresses()  )

    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $address.getPostCode()) )
    {"regexp": "(?i)$approximateMatch", "placeHolder": "PPPPP"},
    #end

    #foreach( $approximateMatch in $StringTools.getApproximatelyMatchingStringList($sourceText, $address.getAddress()) )
    {"regexp": "(?i)$approximateMatch", "placeHolder": "AAAAA"},
    #end

  #end

]
