[
  #foreach( $phone in $patient.getPhoneNumbers()  )
    {"regexp": "(?i)$phone.trim()", "placeHolder": "FFFFF"},
    {"regexp": "(?i)$phone.trim().substring(3)", "placeHolder": "FFFFF"} #if( $foreach.hasNext ), #end
  #end

]
